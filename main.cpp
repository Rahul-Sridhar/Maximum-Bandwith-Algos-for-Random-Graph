#include <bits/stdc++.h>

using namespace std;

#include "Graph.hpp"
#include "Heap.hpp"
#include "Max_BW_Path_without_heap.hpp"
#include "Max_BW_Path_with_heap.hpp"
#include "Kruskal.hpp"

int main()
{
	/*int num_of_vertices=5000, avg_degree=1000;
	Graph* g=new Graph(num_of_vertices);
	vector<vector<pair<int, int> > > graph=g.create_graph(avg_degree);
	//cout<<graph.size();
	for(int i=0; i<graph.size(); i++)
	{
		//cout<<i<<" -> ";
		for(int j=0; j<graph[i].size(); j++)
		{
			//cout<<graph[i][j].first<<" ";
		}
		//cnt+=graph[i].size();
	}*/
    /*Heap* h=new Heap(10);
    h->update_weights(3, 5);
    h->insert_key(5);
    h->update_weights(10, 7);
    h->insert_key(7);
    h->delete_key(0);
	cout<<h->maximum();*/
	return 0;
}




